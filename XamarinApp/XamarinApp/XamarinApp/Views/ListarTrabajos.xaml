<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinApp.Views.ListarTrabajos" 
             Title="Listado de trabajos">

    <ScrollView>

        <StackLayout Orientation="Vertical" Padding="10">

            <ListView x:Name="listTrabajos" IsPullToRefreshEnabled="True" ItemSelected="ListTrabajos_ItemSelected">

                <ListView.ItemTemplate>

                    <DataTemplate>

                        <TextCell Text="{Binding CodTrabajo}" Detail="{Binding Descripcion}" TextColor="Black"/>

                    </DataTemplate>

                </ListView.ItemTemplate>

                <ListView.Footer>

                    <Button x:Name="btnAgregarTrabajo"
                            HorizontalOptions="FillAndExpand"
                            Text="Crear trabajo" Clicked="BtnAgregarTrabajo_Clicked"/>

                </ListView.Footer>

            </ListView>

        </StackLayout>
        
    </ScrollView>

</ContentPage>